name: CD Pipeline (Production Deployment)

on:
  push:
    branches: [master, main]
    tags:
      - "v*.*.*"
  workflow_dispatch:

jobs:
  deploy:
    name: Build & Deploy Production
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Checkout source code
        uses: actions/checkout@v4

      - name: 📦 Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.22.0
          run_install: false

      - name: 🟢 Setup Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: 📥 Install dependencies
        run: pnpm install --frozen-lockfile

      - name: 🗄️ Generate Prisma Client
        run: pnpm prisma generate
        env:
          DATABASE_URL: "file:./dev.db"

      - name: 🏗️ Next.js Production Build
        run: pnpm build
        env:
          NEXT_PUBLIC_APP_ENV: production
          NEXT_PUBLIC_SITE_URL: https://embedded-aiot.ptit.edu.vn
          DATABASE_URL: "file:./dev.db"

      - name: 🐳 Set up Docker Buildx (Optional for Docker Deploy)
        uses: docker/setup-buildx-action@v3

      # Tùy chọn deploy trực tiếp lên VPS qua SSH (nếu bạn cấu hình SSH Secrets trên GitHub Repository)
      # - name: 🚀 Deploy to VPS via SSH
      #   uses: appleboy/ssh-action@v1.0.3
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     script: |
      #       cd /var/www/embedded-lab-web
      #       git pull origin master
      #       pnpm install --frozen-lockfile
      #       pnpm prisma generate
      #       pnpm build
      #       pm2 reload embedded-lab || pm2 start npm --name "embedded-lab" -- start
