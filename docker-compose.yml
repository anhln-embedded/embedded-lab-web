services:
  app:
    image: ghcr.io/anhln-embedded/embedded-lab-web:latest
    container_name: embedded_lab_web
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_ENV=production
      - DATABASE_URL=file:/app/prisma/dev.db
    volumes:
      - lab_db_data:/app/prisma
    networks:
      - lab_network

volumes:
  lab_db_data:
    driver: local

networks:
  lab_network:
    driver: bridge
